apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ipam.fullname" . }}-test-connection"
  labels:
    {{- include "ipam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ipam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
